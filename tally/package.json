{
  "name": "tally",
  "version": "1.0.0",
  "description": "Tally — street fundraiser approach/sign-up counter with shift tracking. Static site deployed with Architect (arc.codes).",
  "private": true,
  "scripts": {
    "start": "arc sandbox",
    "dev": "arc sandbox",
    "deploy": "arc deploy",
    "deploy:production": "arc deploy --production"
  },
  "devDependencies": {
    "@architect/architect": "^11.0.4"
  }
}
